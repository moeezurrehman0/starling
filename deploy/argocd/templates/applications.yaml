{{- if not .Values.repoURL }}
{{- fail "repoURL must be set: ArgoCD cannot infer which repository to track" }}
{{- end }}
{{- if .Values.devInfra }}
---
# The backing services. Sync wave -1, so Redis, Postgres and LocalStack are
# healthy before any application pod starts -- otherwise every service crash-loops
# through its first readiness window and the initial sync reports Degraded for
# reasons that have nothing to do with the applications.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-infra-{{ .Values.env }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    path: deploy/charts/dev-infra
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.namespace }}
  syncPolicy:
    {{- if .Values.autoSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
{{- end }}
{{- if .Values.observability }}
---
# The observability stack. Sync wave -2, ahead of even dev-infra: the collector
# has to exist before the first application pod makes its first export, or that
# export fails and the span for the very first request -- the one most likely to
# be inspected during a demo -- is lost.
#
# Sync is never automated for this Application regardless of .Values.autoSync.
# selfHeal on a monitoring stack means that silencing an alert or widening a
# scrape during an incident is reverted underneath you, minutes later, with no
# notification. Drift here is a smaller problem than losing manual control of
# the tool you are using to diagnose the outage.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-{{ .Values.env }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    path: deploy/charts/observability
    helm:
      parameters:
        - name: tier
          value: {{ .Values.env | quote }}
  destination:
    server: https://kubernetes.default.svc
    # The chart renders into two namespaces of its own. This one is only the
    # destination ArgoCD reconciles against; every resource carries an explicit
    # namespace, and CreateNamespace below applies to both.
    namespace: observability
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
{{- end }}
{{- range .Values.services }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ . }}-{{ $.Values.env }}
  namespace: argocd
  # Without this finalizer, deleting the Application leaves every workload it
  # created running and unowned. That is the quiet failure mode of app-of-apps:
  # the tree looks clean and the cluster is full of orphans.
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/part-of: starling
    starling.io/env: {{ $.Values.env }}
spec:
  project: default
  source:
    repoURL: {{ $.Values.repoURL | quote }}
    targetRevision: {{ $.Values.targetRevision | quote }}
    path: deploy/charts/service
    helm:
      releaseName: {{ . }}
      # Order matters: the environment file first, the service file second, so a
      # service can override an environment default and not the other way round.
      valueFiles:
        - ../../envs/{{ $.Values.env }}/values.yaml
        - ../../envs/{{ $.Values.env }}/{{ . }}.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ $.Values.namespace }}
  syncPolicy:
    {{- if $.Values.autoSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      # Server-side apply. The client-side default stores the whole manifest in a
      # last-applied-configuration annotation, which breaks on large CRDs and
      # fights any other controller that owns a field -- the HPA owning
      # spec.replicas being exactly the case this repo cares about.
      - ServerSideApply=true
  revisionHistoryLimit: 5
  ignoreDifferences:
    # The HPA owns this field. The chart already omits it when autoscaling is on,
    # so this is belt and braces for the window between an HPA being created and
    # the next render -- and it costs nothing to state the ownership explicitly.
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
{{- end }}
